import { TestBed } from '@angular/core/testing';

import { ApiResponse } from './api-interfaces';

describe('ApiInterfaces', () => {
  it('should compile', () => {
    // This is a file containing only TypeScript interfaces,
    // so there are no services or components to test in the traditional sense.
    // This test simply ensures the file is syntactically correct and can be imported.
    expect(true).toBeTruthy();
  });
});